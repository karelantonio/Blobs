# Note on kivymd:

Seems like there is some issue while packaging the KivyMD wheel (the .kv files are not included) so in order to use it we must clone the repository ourselves.

To do so run in your terminal:

```sh
make kivymd
```

TODO: Checkout a specific version instead of the latest commit.
